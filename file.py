#first code
print("Hello World!")
#currently the file's status is untracked.
#To track the file, we need to add it to the repository using the following command:
#git add file.py
#After adding the file, we can commit it using the following command:
#git commit -m "Added file.py"
#Finally, we can push the changes to the remote repository using the following command:
#git push origin master


new code